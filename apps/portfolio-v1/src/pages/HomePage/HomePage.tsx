// libs
import { memo } from 'react';

// styles
import styles from './HomePage.module.scss';

function HomePage() {
    const render = () => {
        return <></>;
    };

    return render();
}

export default memo(HomePage);
